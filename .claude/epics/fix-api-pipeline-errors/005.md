---
issue: 005
name: Write failing test for batch retry logic
status: pending
created: 2025-08-25T19:30:00Z
depends_on: [004]
parallel: false
---

# Task 005: Write failing test for batch retry logic

## Context
Need robust retry logic for transient API failures to improve reliability.

## Objective
Create tests that verify:
1. Transient failures trigger retries
2. Exponential backoff is implemented
3. Max retry limits are enforced
4. Partial batch success is handled

## Implementation Plan

### 1. Create test file: `test/test_batch_retry_logic.py`
```python
import pytest
import asyncio
from unittest.mock import Mock, patch, call
from api.google_embedding_client import GoogleEmbeddingClient

class TestBatchRetryLogic:
    """Test suite for batch embedding retry logic"""
    
    @pytest.mark.asyncio
    async def test_transient_failure_triggers_retry(self):
        """Test that transient API failures trigger retries"""
        # Mock API to fail then succeed
        # Assert retry occurs
        # Assert final success
        
    def test_exponential_backoff_timing(self):
        """Test exponential backoff between retries"""
        # Mock time.sleep
        # Trigger retries
        # Assert sleep called with: 1, 2, 4, 8 seconds
        
    def test_max_retry_limit_enforced(self):
        """Test that retries stop after max attempts"""
        # Mock API to always fail
        # Assert exactly 3 retries (configurable)
        # Assert final failure raised
        
    def test_partial_batch_success_preserved(self):
        """Test that successful embeddings in batch are kept"""
        # Mock batch with 50% success rate
        # Assert successful embeddings preserved
        # Assert only failed ones retried
```

### 2. Test Scenarios
- API returns 503 (Service Unavailable)
- API returns 429 (Rate Limited)
- Network timeout
- Partial batch with some nulls
- Intermittent failures that recover

### 3. Expected Failures (Red Phase)
Tests should fail because:
- No retry logic currently implemented
- Failed requests not retried
- No exponential backoff
- Batch failures are terminal

## Success Criteria
- [ ] Test file with retry scenarios
- [ ] Tests fail showing no retry logic
- [ ] Tests demonstrate need for backoff
- [ ] Clear assertions about retry behavior
- [ ] Coverage of common failure modes

## Verification Commands
```bash
# Run retry logic tests
pytest test/test_batch_retry_logic.py -v

# Check for retry attempts
pytest test/test_batch_retry_logic.py -v 2>&1 | grep "retry"

# Test with simulated network issues
python -c "import requests; 
# Simulate network failures and check retry behavior"
```

## Notes
- RED phase for retry logic
- Focus on resilience patterns
- Test timing and backoff
- Document retry strategies