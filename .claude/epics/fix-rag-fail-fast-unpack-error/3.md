---
name: Standardize RAG.call() to always return (answer_or_none, docs)
status: open
created: 2025-08-26T12:46:12Z
updated: 2025-08-26T13:27:00Z
github: https://github.com/zawarudo/deepwiki-open/issues/3
depends_on: []
parallel: true
conflicts_with: []
---

# Task 002: Standardize RAG.call() to always return (answer_or_none, docs)

## Description

Fix RAG.call() to ensure it always returns consistent tuple format. Add __call__ method for syntactic sugar. Handle empty/None queries with early return. Catch all exceptions and return (None, []) to prevent unpacking errors in calling code.

## Phase

1.1

## Acceptance Criteria

- [ ] RAG.call() always returns tuple format (answer_or_none, docs)
- [ ] Add __call__ method that delegates to call() method
- [ ] Handle empty/None queries with early return (None, [])
- [ ] Catch all exceptions and return (None, []) instead of propagating
- [ ] Maintain backward compatibility with existing callers
- [ ] All return paths verified to return tuples
- [ ] Exception handling prevents unpacking errors

## Technical Details

The RAG.call() method should:
- Always return a tuple in format (answer_or_none, docs)
- Handle edge cases like empty queries, None inputs
- Provide comprehensive exception handling
- Add __call__ method for cleaner syntax (rag_instance() instead of rag_instance.call())
- Ensure consistent return type across all code paths
- Return (None, []) for any error conditions instead of raising exceptions
- Maintain existing functionality while fixing return format consistency

## Dependencies

- Current RAG implementation
- Existing callers of RAG.call() method
- Error handling patterns in codebase

## Effort Estimate

1 hour

## Test File

test/test_rag_tuple_return.py

## Definition of Done

- [ ] RAG.call() method updated to always return tuple format
- [ ] __call__ method added for syntactic sugar
- [ ] Empty/None query handling implemented
- [ ] Comprehensive exception handling added
- [ ] All return paths return (answer_or_none, docs) tuple
- [ ] Backward compatibility maintained
- [ ] Test suite covers all edge cases and error conditions
- [ ] No unpacking errors in calling code
- [ ] Code review completed
- [ ] Integration tests pass